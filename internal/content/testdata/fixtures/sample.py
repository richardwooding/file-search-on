# Sample Python fixture for the source content-type family.
# Exercises hash-comment-only counting (no block syntax in the
# simple language model).

def greet(name):
    # Inner comment.
    print(f"hello, {name}")


def add(a, b):
    return a + b
